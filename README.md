# Meu Site Flask

Um site simples feito com Flask, pronto para deploy no Render.

## Como rodar localmente

```bash
python -m venv venv
source venv/bin/activate  # Não estou usando Ambiente Virtual
pip install -r requirements.txt
python app.py
